<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mingbo.mapper.AuthorityRequestMapper">
    <!-- 定义resultMap处理驼峰映射 -->
    <resultMap id="authorityResultMap" type="com.mingbo.pojo.AuthorityRequest">
        <id property="id" column="id" />
        <result property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="adminId" column="admin_id"/>
        <result property="adminName" column="adminname"/>
        <result property="requestMessage" column="request_message"/>
        <result property="status" column="status"/>
        <result property="permCreate" column="perm_create"/>
        <result property="permRead" column="perm_read"/>
        <result property="permUpdate" column="perm_update"/>
        <result property="permDelete" column="perm_delete"/>
        <result property="createdAt" column="created_at"/>
        <result property="processedAt" column="processed_at"/>
    </resultMap>

    <select id="getReceivedRequestsByPage" resultMap="authorityResultMap">
        SELECT tb_authorization_request.*, user1.username AS username, user2.username AS adminname
        FROM tb_authorization_request, tb_user AS user1, tb_user AS user2
        WHERE admin_id=#{id} AND user1.id=tb_authorization_request.user_id
            AND user2.id=tb_authorization_request.admin_id
        ORDER BY id DESC
        LIMIT #{begin}, #{size}
    </select>
    <select id="getSentRequestsByPage" resultType="com.mingbo.pojo.AuthorityRequest">
        SELECT tb_authorization_request.*, user1.username AS username, user2.username AS adminname
        FROM tb_authorization_request, tb_user AS user1, tb_user AS user2
        WHERE user_id=#{id} AND user1.id=tb_authorization_request.user_id
          AND user2.id=tb_authorization_request.admin_id
        ORDER BY id DESC
        LIMIT #{begin}, #{size}
    </select>
</mapper>