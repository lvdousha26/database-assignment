# 项目收获、体会与展望

## 一、项目概况

本项目是一个完整的采油厂油水井作业成本管理系统，从零搭建到功能完善，经历了需求分析、架构设计、编码实现、安全加固等多个阶段。技术栈选用 Spring Boot 3 + Vue 3 前后端分离架构，涵盖油水井管理、作业管理、成本核算（三级审批）、数据可视化、AI 智能分析、消息通信、权限控制等核心功能。

---

## 二、收获

### 2.1 全栈工程能力

**后端方面**，深入实践了 Spring Boot 3 的完整开发流程：
- 通过 `@RestControllerAdvice` 构建统一异常处理体系，区分业务异常与系统异常，使前端能精准获知错误原因
- 利用 AOP 切面实现了三个关注点分离的横切逻辑：`@Log` 操作日志、`@AutoCache` Redis 缓存、`@RateLimit` 接口限流，不侵入业务代码
- MyBatis 注解模式下的动态 SQL 编写（`<script>` 标签 + `<if>` 条件判断），在保持 SQL 完全可控的同时实现了灵活查询

**前端方面**，掌握了 Vue 3 Composition API 的工程化实践：
- Pinia + persist 实现登录状态的持久化，页面刷新不丢失
- Axios 请求/响应拦截器统一处理 token 携带、错误提示、未登录跳转
- 结合 Element Plus 的表单校验、分页、对话框等组件构建企业级 CRUD 界面

### 2.2 安全体系设计

双重防护权限系统是本项目最大的收获：

- **应用层**：`PermissionService` 的 CRUD 细粒度校验，管理员自动放行，普通用户按位 OR 聚合多管理员授权
- **数据库层**：`app_secure` 用户 + GRANT/REVOKE + 6 个业务视图，形成最小权限原则的纵深防御
- **前端配合**：`usePermission` composable 让按钮根据权限显隐，与后端校验形成完整闭环

实践中深刻体会到：安全不是单点控制，而是需要在每一层都设防——前端按钮隐藏防止误操作，后端 Service 校验防止 API 直接调用，数据库用户限制防止直连越权。

### 2.3 Redis 在项目中的多面角色

同一个 Redis 实例承担了多种用途，展示了缓存的灵活性：
- `@AutoCache` 注解：方法级结果缓存，减少数据库重复查询
- `@RateLimit` 注解：基于 Redis INCR + TTL 实现滑动窗口限流
- `online:users` 集合：记录在线用户，5 分钟过期自动清理

### 2.4 AI 集成经验

接入 DeepSeek Chat API 并支持工具调用（ToolDefinition / ToolExecutor），让我理解了 LLM 与业务系统结合的基本模式：
- System Prompt 定义 AI 的角色和行为边界
- 工具调用让 AI 能查询数据库、分析数据
- API Key 由用户自行配置，避免了服务端 Key 泄露风险

---

## 三、体会

### 3.1 分层架构的价值

项目采用标准的三层架构（Controller → Service → Mapper），加上 AOP 切面和拦截器，形成了清晰的关注点分离：

```
拦截器（认证）→ Controller（路由 + 权限校验）→ Service（业务逻辑）→ Mapper（数据访问）
                                                                        ↓
                                                                  AOP（日志/缓存/限流）
```

这种分层在后续迭代中展现了巨大价值：增加权限校验只需在 Controller 加一行 `permissionService.requireXxx()`；增加缓存只需在 Service 方法上加 `@AutoCache`；修改数据源不影响上层业务逻辑。

### 3.2 权限系统的复杂性

权限系统从最初简单的"有无权限"发展到精细的 CRUD 四维控制，再到申请-审批流程，逐步暴露了其复杂性：
- 用户可以从多个管理员获得权限，需要 OR 聚合而非简单覆盖
- 前端按钮显隐 + 后端 API 校验 + 数据库层 GRANT 三层防护各自有不同粒度
- 权限申请需要消息通知联动，涉及两个模块的交互

一个教训是：权限系统应该在项目初期就设计好数据模型，后期再追加权限控制会导致需要回溯修改所有 Controller。

### 3.3 前端布局的双轨制

项目存在两套前端布局（管理员端和用户端），共用大部分业务组件。这种设计既满足了不同角色的界面差异需求，又通过组件复用避免了代码重复。关键经验：
- 路由配置明确区分 `/admin/*` 和 `/user/*`，登录时根据角色自动分流
- 侧边栏菜单项不同，但页面组件可以共用（如 WellManagement.vue 在两个布局中都使用）
- 毛玻璃（glassmorphism）设计风格保持两套布局的视觉一致性

### 3.4 安全是持续的过程

项目经历了从"无权限→有权限→安全加固"的演进：
1. 初始阶段：所有操作开放，无权限控制
2. 中期：加入权限数据模型，记录谁有什么权限，但不做强制校验
3. 后期：`PermissionService` 与 Controller 绑定，真正拦截未授权操作
4. 安全审查：修复 `DEBUG_MODE` 硬编码、SQL 注入排查、按钮防抖、登录限流

这个过程说明：安全不是一蹴而就的，需要在迭代中持续审视和改进。

---

## 四、展望

### 4.1 短期可改进项

| 项目 | 说明 | 优先级 |
|------|------|--------|
| 操作日志审计页面 | `tb_operate_log` 表已有数据，需要管理端查看界面 | 高 |
| 文件管理完善 | ResourceController 已有上传/列表/删除，界面可进一步美化 | 中 |
| 消息已读回执 | 当前仅标记已读，可增加已读时间戳 | 低 |

### 4.2 中期规划

**单元测试覆盖**：后端核心 Service（PermissionService、AuthorityService）缺少单元测试，关键路径依赖人工验证。应引入 JUnit 5 + Mockito 建立基础测试体系。

**CI/CD 流水线**：目前部署依赖手动操作（Vercel + Railway），可接入 GitHub Actions 实现：
- 推送 main 分支自动运行测试
- 测试通过后自动部署前端到 Vercel、后端到 Railway

**前端性能优化**：
- Vite 代码分割（按路由懒加载）
- ECharts 图表按需导入，减小首屏包体积
- 列表页虚拟滚动处理大量数据

### 4.3 远期方向

**微服务化**：当业务规模增长时，可将三个核心模块拆分为独立服务：
- 油水井基础数据服务（Well Service）
- 作业成本核算服务（Cost Service）
- 消息通知服务（Message Service）

各服务通过消息队列（RabbitMQ/Kafka）异步通信，降低耦合。

**数据仓库与分析**：成本数据累计到一定规模后，可引入 OLAP 分析：
- 按时间、油田、作业类型等多维度分析成本趋势
- 预警机制：成本超预算自动告警
- 报表自动生成（月报/季报/年报）

**移动端适配**：当前仅适配桌面端，可基于同一套 API 开发：
- 移动端 H5（基于 Vant 或 NutUI）
- 微信小程序
- 钉钉/飞书集成（消息通知推送到办公平台）

---

## 五、总结

这个项目从最初的一个简单 CRUD 应用，逐步成长为一个具备完善权限体系、AI 能力、数据可视化、安全防护的企业级管理系统。最大的收获不是代码行数，而是对"工程化"的理解：

- **好的架构不是设计出来的，是演进出来的**——分层架构让每一步迭代都站在前一步的基础上
- **安全是纵深防御，不是单点控制**——前端、后端、数据库三层各司其职
- **代码是负债，设计是资产**——花时间在抽象和分层上，后续迭代会十倍回报

技术会过时，但工程思维和方法论可以复用。这段经历为后续更复杂的系统设计打下了扎实的基础。
